# bridgeUI 성능 평가 보고서

> 노년층 디지털 리터러시 보조 — 실시간 UI 번역 플러그인
> 최종 갱신: 2026-06-24 | 현재 단계: Step 10 (최적화·성과 분석)

---

## 1. 시스템 개요

bridgeUI는 노년층 사용자가 복잡한 앱(지도·배달·예매)의 아이콘·버튼 기능을 직관적으로 이해하도록 돕는 실시간 UI 설명 도구다. 사용자가 화면의 UI 요소를 탭하면 "이것을 누르면 무슨 일이 일어나는지"를 노년층 친화적 문장으로 말풍선에 표시한다.

핵심은 **3단계 캐시 파이프라인**으로, 비용·속도·정확도를 단계적으로 최적화한다.

### 1.1 Full Pipeline

```
사용자 탭
  │
  ▼
[YOLO 탐지]  전체 스크린샷 → 크롭 중심에 가장 가까운 UI 박스 선택 (element_image)
  │             (탐지 실패 시 fallback=True — 사용자 크롭 좌표 직접 사용)
  │
  ▼
[앱 카테고리 결정]  _get_category_packages(app_package) → 같은 카테고리 앱 목록 확보
  │                 예) 카카오맵 → [네이버지도, 카카오맵, 구글지도, 티맵]
  │
  ▼
[STAGE 1 — HASH]  pHash 해밍거리 ≤ 8(동일앱) / ≤ 4(크로스앱) ?
  ├─ YES ──▶ 즉시 반환 (<1ms), cross_app 플래그 포함
  │ NO
  ▼
[STAGE 2 — FAST]  CLIP 코사인 유사도 ≥ 0.90(동일앱) / ≥ 0.95(크로스앱) ?
  ├─ YES ──▶ 캐시 반환 (~80ms), cross_app 플래그 포함
  │ NO
  ▼
[STAGE 3 — DEEP]  Gemini 2.5 Flash Vision 분석 (1~3s)
  │   - 전체 스크린샷 + 요소 이미지 동시 입력 (맥락 기반 분석)
  │   - 3회 지수 백오프 재시도 (3s, 6s)
  │   - 종결어미·최소길이 검증 (len≥20, 끝글자∈'.요다')
  ├─ 성공 → ChromaDB 저장 + pHash 즉시 등록 → 반환
  └─ 실패 → "정보를 찾는 중입니다" (DB 저장 없음)
  ▼
말풍선 표시 (track 배지 + 설명)
```

### 1.2 3단계 트랙 구분

| 트랙 | 기술 | 조건 (동일앱) | 조건 (크로스앱) | 속도 |
|------|------|--------------|----------------|------|
| **HASH** | Perceptual Hash | 해밍거리 ≤ 8 | ≤ 4 | <1ms |
| **FAST** | CLIP 코사인 유사도 | ≥ 0.90 | ≥ 0.95 | ~80ms |
| **DEEP** | Gemini 2.5 Flash Vision | 캐시 미스 시 | — | 1~3s |

> **크로스앱 이중 임계값**: 다른 앱 항목과 매칭 시 엄격한 임계값을 적용해 오탐 방지.
> 모든 신규 분석(DEEP)은 자동 캐싱되어 두 번째 요청부터 HASH/FAST로 전환된다.

---

## 2. 평가 기준 체계

### I. 파이프라인 효율성 (정량)

| 지표 | 정의 | 측정 방법 | 목표 |
|------|------|-----------|------|
| **Stage별 히트율** | HASH / FAST / DEEP / Error 각 비율 | 동일 앱 반복 탭 기록 | HASH+FAST ≥ 70% |
| **HASH 응답 시간** | pHash 히트 시 서버 왕복 | 클라이언트 타임스탬프 차이 | < 50ms (네트워크 포함) |
| **FAST 응답 시간** | CLIP 히트 시 서버 왕복 | 동일 | < 200ms |
| **DEEP 응답 시간** | Gemini 분석 포함 총 시간 | 동일 | < 6s |
| **DB 무결성** | 설명이 잘리지 않은 항목 비율 | `len(desc)<20` or 종결어미 미충족 개수 / 전체 | ≥ 95% |
| **캐시 자기완결성** | DEEP → 다음 탭에서 HASH 히트 여부 | 동일 UI 2회 연속 탭 | 2번째는 반드시 HASH |

### II. 크로스앱 캐시 (핵심 아이디어 검증)

> "같은 카테고리(지도앱)의 같은 역할·유사 모양 UI는 같은 설명을 출력한다"

| 지표 | 정의 | 측정 시나리오 | 목표 |
|------|------|--------------|------|
| **카테고리 내 재사용률** | A앱 캐시를 B앱이 Hit하는 비율 | 네이버맵 저장 후 카카오맵 동일 UI 탭 | Cross-Hit 1개 이상 확인 |
| **설명 일관성** | 같은 역할 UI의 설명이 동일한지 | 3개 지도앱 길찾기 버튼 비교 | 동일 문장 출력 |
| **오탐율** | 다른 기능인데 같은 설명이 나오는 비율 | 상이한 앱의 유사 모양 UI 탭 | < 10% |

### III. 탐지 정확도 (YOLO / OmniParser)

| 지표 | 정의 | 측정 방법 | 목표 |
|------|------|-----------|------|
| **탐지 성공률** | 크롭 내 UI 정상 탐지 비율 | `fallback=False` 응답 수 / 전체 | ≥ 80% |
| **bbox 정확도** | 탐지 박스가 실제 아이콘을 포함 | 뷰어 이미지 육안 확인 | 주관 판정 |
| **conf 분포** | 탐지 confidence 분포 | detection.confidence 로그 | 평균 ≥ 0.4 |

### IV. 설명 품질 (정성, 5점 루브릭)

| 항목 | 1점 | 3점 | 5점 |
|------|-----|-----|-----|
| **목적 중심성** | 기능 명칭만 설명 | 동작 포함하나 모호 | "누르면 ~됩니다" 명확 |
| **어휘 접근성** | 전문 용어 포함 | 혼용 | 일상어만 사용 |
| **맥락 일치도** | 화면 맥락과 불일치 | 부분 일치 | 전체 화면 기준 정확 |
| **간결성** | 3문장 이상 | 2문장 초과 | 2문장 이내 |

> 평가 대상: DEEP 트랙 출력 10개 샘플 (앱 3개 × 3~4개 UI)

### V. 테스트 시나리오 (Full Pipeline)

```
TC-01  캐시 자기완결성
  신규 UI 탭(DEEP) → 즉시 동일 UI 재탭 → HASH 히트 확인
  판정: HASH 응답 및 동일 설명 여부

TC-02  크로스앱 캐시 공유 (핵심)
  네이버지도 길찾기 버튼 탭 → 카카오맵 길찾기 → 구글맵 길찾기
  판정: 3개 모두 동일 설명 & HASH/FAST 트랙

TC-03  Seed DB 사전 로드 효과
  서버 시작 직후 seed DB 포함 UI 탭
  판정: 첫 요청부터 HASH 히트 (DEEP 호출 없음)

TC-04  DB 오염 방지
  Gemini 실패 유발 후 응답 확인
  판정: "정보를 찾는 중입니다" 반환 & DB count 변화 없음

TC-05  재분석 기능
  잘못된 설명 확인 → 재분석 버튼 → 동일 UI 재탭
  판정: 새 설명으로 HASH 히트
```

---

## 3. 현재 모델 성능 한계 분석

> 면담·시연 전 솔직히 짚어야 할 미흡 지점. 각 항목에 원인과 개선 방향을 함께 명시한다.

### 3.1 탐지 단계 (YOLO / OmniParser)

| 한계 | 원인 | 영향 | 개선 방향 |
|------|------|------|----------|
| **모양이 크게 다른 동일 기능 UI 미공유** | 구글맵 길찾기(다이아몬드)와 네이버맵(원형) 형태 차이로 CLIP 유사도 < 0.90 | 크로스앱 캐시가 "모양이 비슷할 때만" 작동 | 역할 기반 임베딩 또는 텍스트 라벨 보조 매칭 |
| **conf 임계값 민감성** | 전체 스크린샷 conf 0.05, /detect conf 0.3로 이원화 | 작은 아이콘 누락 또는 과탐지 | 화면 밀도별 적응형 임계값 |
| **fallback 의존** | YOLO 탐지 실패 시 사용자 크롭 그대로 사용 | bbox 부정확 → pHash·CLIP 정확도 저하 | fallback 비율 측정 후 탐지 보강 |

### 3.2 캐시 단계 (pHash / CLIP)

| 한계 | 원인 | 영향 | 개선 방향 |
|------|------|------|----------|
| **유사도 임계값 트레이드오프** | 동일앱 0.90 / 크로스앱 0.95 — 낮추면 히트율↑ 오탐↑ | 요소 유형별 최적값 상이 | 요소 유형별 추가 차등 임계값 |
| **카테고리 수동 정의** | `_APP_CATEGORIES` 하드코딩 (4개 그룹) | 신규 앱 추가 시 수동 갱신 필요 | 패키지 메타데이터 기반 자동 분류 |
| **앱 간 의미 충돌 위험** | 같은 화살표가 앱별로 다른 의미 가능 | 크로스앱 공유 시 오탐 가능성 | 이중 임계값으로 완화됨, 역할 검증 추가 예정 |

### 3.3 분석 단계 (Gemini 2.5 Flash)

| 한계 | 원인 | 영향 | 개선 방향 |
|------|------|------|----------|
| **응답 잘림(truncation)** | thinking 토큰 소비로 출력 토큰 부족 | 불완전 문장 → 종결어미 검증으로 재시도 | max_output_tokens 상향(300), 검증 로직 적용 중 |
| **503 불안정** | 무료 티어 가용성 변동 | 간헐적 분석 실패 | 3회 지수 백오프 재시도 적용 중 |
| **맥락 오해석** | 요소 단독으로는 역할 모호 | 잘못된 설명 캐싱 위험 | 전체 스크린샷 동시 입력으로 완화 |
| **무료 티어 한계** | 분당/일일 요청 제한 | 대량 사용 시 병목 | 유료 전환 또는 온디바이스 경량 모델 검토 |

### 3.4 데이터 측면

| 한계 | 현황 | 개선 방향 |
|------|------|----------|
| **seed DB 규모** | 3개 앱 94개 요소 (배민·네이버지도·코레일) | 대상 앱·요소 확대 |
| **실측 데이터 부재** | 히트율·응답시간 실기기 측정 미완 | 본 보고서 §4 수집 계획으로 확보 |
| **품질 평가 미정량화** | 설명 품질 주관 판단 의존 | 루브릭 기반 다수 평가자 채점 |

### 3.5 조치 보고 (§3 한계 대응)

> 위 한계에 대해 이미 코드에 반영한 조치(✅ 완료)와 다음 단계 조치(🔄 진행 / ⬜ 예정)를 구분해 보고한다.

#### (1) 분석 단계 — 가장 시급했던 항목, 대부분 조치 완료

| 한계 | 조치 내용 | 구현 위치 | 상태 |
|------|-----------|-----------|------|
| 응답 잘림 | 종결어미·최소 길이 검증 후 불완전 시 재시도 (`len<20 or 끝글자∉'.요다'`) | `deep_track.py:123` | ✅ 완료 |
| 응답 잘림 | `max_output_tokens` 150 → 300 상향 (thinking 토큰 여유 확보) | `deep_track.py:23` | ✅ 완료 |
| 503 불안정 | 3회 지수 백오프 재시도 (3s → 6s) | `deep_track.py:114-129` | ✅ 완료 |
| 맥락 오해석 | 전체 스크린샷 + 요소 이미지 동시 입력으로 화면 맥락 제공 | `deep_track.py:89-101` | ✅ 완료 |
| 잘못된 설명 캐싱 | 분석 실패 시 `None` 반환 → DB 저장 차단 (오염 방지) | `main.py:206-217` | ✅ 완료 |
| 잘못된 설명 정정 | `/db/reanalyze` — 캐시된 오답을 강제 재분석 후 덮어쓰기 | `main.py:320-394` | ✅ 완료 |
| 무료 티어 한계 | 유료 전환 또는 온디바이스 경량 VLM 검토 | — | ⬜ 예정 |

#### (2) 캐시 단계 — 크로스앱 공유 + 오탐 방지 이중 임계값 완료

| 한계 | 조치 내용 | 구현 위치 | 상태 |
|------|-----------|-----------|------|
| 앱 간 캐시 미공유 | 앱 카테고리 그룹(`_APP_CATEGORIES`) 도입 — 지도앱끼리 Stage 1/2 캐시 공유 | `main.py:30-60` | ✅ 완료 |
| 다중 패키지 검색 | ChromaDB `$in` 연산자로 카테고리 전체 동시 조회 | `fast_track.py:30-35` | ✅ 완료 |
| pHash 앱 필터 | 해밍 비교 시 카테고리 패키지 집합으로 필터 | `hash_track.py:100-103` | ✅ 완료 |
| 앱 간 의미 충돌 | **이중 임계값** — 크로스앱 히트에 해밍 ≤ 4 / 유사도 ≥ 0.95 적용 | `hash_track.py:21` `fast_track.py:5` | ✅ 완료 |
| cross_app 추적 | 응답에 `cross_app` 플래그 포함 — 히트 출처 가시화 | `main.py`, `hash_track.py`, `fast_track.py` | ✅ 완료 |
| 유사도 임계값 고정 | 요소 유형별 추가 차등 임계값 (icon/button/text 분리) | — | ⬜ 예정 |
| 카테고리 수동 정의 | 패키지 메타데이터 기반 자동 분류 | — | ⬜ 예정 |

#### (3) 탐지 단계 — conf 측정 도구 완비, 핵심 한계 잔존

| 한계 | 조치 내용 | 상태 |
|------|-----------|------|
| 모양 다른 동일 기능 UI 미공유 | **(미해결 핵심)** CLIP은 시각 유사도 기반이라 형태 차이를 넘지 못함. → 역할 기반 텍스트 라벨 보조 매칭 또는 element_type+동작 키워드 결합 검색 설계 | ⬜ 예정 |
| conf 임계값 민감성 | `test_detector.py` 3개 섹션으로 재편 — 합성 스윕 + **실제 seed 스크린샷 conf 분포(mean/min/max/구간)** + `/capture`(0.05) vs `/detect`(0.30) 비교 | ✅ 완료 |
| fallback 추적 | 응답에 `fallback` 필드 추가 — YOLO 탐지 실패율 실측 가능 | ✅ 완료 |
| fallback 파라미터 보강 | fallback 실측값 기반 conf·margin 튜닝 | ⬜ 예정 (§4 계측 후) |

#### (4) 데이터 측면 — §4 수집 계획으로 일괄 대응

| 한계 | 조치 내용 | 상태 |
|------|-----------|------|
| 실측 데이터 부재 | §4 4주 로드맵으로 히트율·응답시간·트랙분포 확보 | 🔄 진행 |
| 품질 미정량화 | §2-IV 5점 루브릭으로 DEEP 출력 10개 채점 | ⬜ 예정 |
| seed DB 규모 | 카테고리별 대상 앱·요소 확대 | ⬜ 예정 |

#### 조치 요약

- **완료(✅)**: 분석 단계 안정화(재시도·토큰·검증·맥락), DB 오염 차단, 재분석 정정 기능, 크로스앱 캐시 공유, **크로스앱 오탐 방지 이중 임계값**, fallback 추적, conf 측정 도구.
- **예정(⬜)**: **모양 다른 동일 기능 UI의 역할 기반 매칭**(최우선 미해결), 요소 유형별 차등 임계값, 자동 카테고리 분류, 모델 티어 확장.

> **핵심**: 신뢰성(분석 실패·오염)·효율(크로스앱 공유)·안전성(오탐 방지 이중 임계값)은 코드 레벨 완료.
> 남은 유일한 본질적 한계는 "시각 형태가 달라도 기능이 같은 UI"를 묶는 것으로, CLIP 임베딩만으로는 불가능하며 **역할 기반 매칭**이라는 별도 설계가 필요하다.

---

## 4. 데이터 수집 계획

> Step 10(성과 분석)의 핵심. "측정 없이는 개선 없다" — 위 평가 기준을 채울 실측 데이터 확보 절차.

### 4.1 수집 항목 및 방법

| 데이터 | 수집 위치 | 방법 |
|--------|-----------|------|
| **트랙 분포** | 서버 `/capture` 응답 | 매 요청 `track` 값 로그 적재 |
| **단계별 응답 시간** | 서버 내부 + 클라이언트 | 각 stage 진입/종료 타임스탬프 측정 |
| **탐지 결과** | `detect_from_full_screenshot()` | `fallback`, `confidence`, `element_type` 로그 |
| **유사도/해밍 분포** | Stage 1/2 | `hamming`, `similarity` 값 누적 |
| **DB 무결성** | ChromaDB 전수 검사 | 종결어미·길이 검증 스크립트 정기 실행 |
| **사용성 피드백** | 노년층 사용자 테스트 | 시나리오별 이해도 설문 |

### 4.2 수집 단계 (4주 로드맵)

```
1주차 — 계측 코드 삽입
  • /capture에 구조화 로그(JSON Lines) 추가: {ts, track, latency_ms, conf, fallback, app}
  • DB 무결성 점검 스크립트 작성 (tests/check_db_integrity.py)

2주차 — 통제 환경 측정
  • 3개 지도앱 × 각 10개 UI = 30개 시나리오 반복 탭
  • TC-01~TC-05 자동/반자동 실행, 트랙·시간 기록
  • 크로스앱 캐시 Cross-Hit 횟수 집계

3주차 — 실사용 측정
  • 실기기(Galaxy S23)에서 자유 탐색 50~100회 캡처
  • 트랙 분포·응답시간 실측 → 목표치 대비 평가
  • DEEP 출력 10개 샘플 추출 → 품질 루브릭 채점

4주차 — 사용성 테스트 + 분석
  • 노년층 참가자 N명: 시나리오 수행 + 이해도 5점 척도
  • 전체 지표 집계 → 목표 달성/미달 표 작성
  • 임계값(0.90 / 8) 튜닝 실험 → 히트율-오탐 곡선 도출
```

### 4.3 측정 도구 정비 (기존 자산 활용)

| 도구 | 현재 역할 | 추가 필요 |
|------|-----------|----------|
| `tests/run_full_test.py` | seed + Stage 검증 | 반복 측정·집계 모드 |
| `tests/test_detector.py` | OmniParser 탐지 진단 | conf 임계값 스윕 |
| `tools/visualize_detections.py` | bbox 시각화 | bbox 정확도 평가 보조 |
| 캡처 뷰어 (`/`) | 실시간 결과 확인 | 트랙·시간 누적 통계 패널 |

### 4.4 산출물

- **정량 대시보드**: 트랙 분포 파이차트, 단계별 응답시간 박스플롯, 히트율 추이
- **임계값 튜닝 곡선**: HAMMING_THRESHOLD / SIMILARITY_THRESHOLD 변화에 따른 히트율-오탐 트레이드오프
- **품질 루브릭 결과표**: DEEP 출력 10개 × 4항목 채점
- **사용성 리포트**: 시나리오별 이해도·완료율

---

## 5. 요약

| 영역 | 현재 상태 | 다음 단계 |
|------|-----------|----------|
| 파이프라인 | 3단계 캐시 + Gemini DEEP 완성 | 실측 히트율 확보 |
| 크로스앱 캐시 | 카테고리 기반 공유 구현 | 모양 차이 큰 UI 매칭 개선 |
| 데이터 | seed 94개, 실측 미완 | 4주 수집 계획 실행 |
| 품질 | 종결어미·길이 검증 적용 | 루브릭 정량 평가 |

> **핵심 메시지**: 시스템 골격(3단계 캐시·크로스앱 공유·자동 캐싱)은 완성됐다. 남은 과제는 **실측 데이터로 효과를 증명**하고, **모양이 다른 동일 기능 UI의 캐시 공유**라는 마지막 정확도 한계를 넘는 것이다.
